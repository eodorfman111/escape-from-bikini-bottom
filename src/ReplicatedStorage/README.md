This folder can contain modules and assets shared between the client and server.

Currently, Stage 1 does not require any shared modules, but as you build more stages you can place reusable code here.  
